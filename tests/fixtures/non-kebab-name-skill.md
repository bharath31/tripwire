---
name: MyHelper
description: Use when testing the --fix flag against a non-kebab-case name
---

Body content with a `code` example. This fixture exists specifically to exercise the
`name-kebab-case` auto-fix path in the CLI's `lint --fix` integration test, so it needs
enough words in here to clear the body length floor and keep every other lint rule quiet,
leaving only the one fixable issue for the test to assert on cleanly.
